# Archivo de configuración para desarrollo local
# Copia este archivo como .env y completa con tus credenciales reales

# ===========================================
# CONFIGURACIÓN DE BASE DE DATOS MYSQL
# ===========================================
# Configuración para XAMPP local
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=construccion_db
DB_PORT=3306

# ===========================================
# CONFIGURACIÓN DE SEGURIDAD JWT
# ===========================================
# Genera una clave secreta segura para JWT
# Puedes usar: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=tu_clave_secreta_jwt_muy_segura_de_64_caracteres_minimo

# Tiempo de expiración del token en minutos
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ===========================================
# CONFIGURACIÓN DEL SERVIDOR
# ===========================================
# Host y puerto del servidor FastAPI
API_HOST=0.0.0.0
API_PORT=8000

# Modo debug (True para desarrollo, False para producción)
DEBUG=True

# ===========================================
# CONFIGURACIÓN DE EMAIL (OPCIONAL)
# ===========================================
# Configuración SMTP para envío de emails
# Descomenta y completa si quieres habilitar notificaciones por email

# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=tu_email@gmail.com
# SMTP_PASSWORD=tu_password_de_aplicacion
# SMTP_FROM_EMAIL=tu_email@gmail.com
# SMTP_FROM_NAME=Sistema de Construcción

# ===========================================
# CONFIGURACIÓN DE LA EMPRESA
# ===========================================
# Información de tu empresa (se puede usar en el frontend)
COMPANY_NAME=Construcciones & Proyectos
COMPANY_EMAIL=info@construccion.com
COMPANY_PHONE=+57 300 123 4567
COMPANY_ADDRESS=Calle 123 #45-67, Ciudad

# ===========================================
# CONFIGURACIÓN DE CORS
# ===========================================
# Dominios permitidos para CORS (separados por comas)
# Para desarrollo local, usa: http://localhost:8000,http://127.0.0.1:8000
ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# ===========================================
# CONFIGURACIÓN DE LOGS
# ===========================================
# Nivel de logging (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Archivo de logs (opcional, si no se especifica usa consola)
# LOG_FILE=logs/app.log

# ===========================================
# CONFIGURACIÓN DE PAGINACIÓN
# ===========================================
# Número máximo de resultados por página
MAX_ITEMS_PER_PAGE=100

# ===========================================
# CONFIGURACIÓN DE ARCHIVOS
# ===========================================
# Tamaño máximo para upload de archivos (en bytes)
MAX_FILE_SIZE=5242880  # 5MB

# Tipos de archivo permitidos (separados por comas)
ALLOWED_FILE_TYPES=jpg,jpeg,png,pdf,doc,docx

# Directorio para archivos subidos
UPLOAD_DIRECTORY=static/uploads